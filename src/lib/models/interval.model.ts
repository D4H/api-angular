/**
 * Interval Unit
 * =============================================================================
 * Some business objects (Inspection) have repeating periods.
 */

export enum Interval {
  Day = 'DAY',
  Month = 'MONTH',
  Year = 'YEAR'
}
